============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install django-sqlalchemy
    $ pip install django-sqlalchemy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sqlalchemy
    $ pip install django-sqlalchemy
